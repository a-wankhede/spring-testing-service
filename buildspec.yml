version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto21

  build:
    commands:
      - echo "Building application..."
      - chmod +x mvnw
      - ./mvnw clean packag -DskipTests

artifacts:
  files:
    - target/*.jar

cache:
  paths:
    - /root/.m2/**/*
